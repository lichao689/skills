interface:
  display_name: "Solo Ship"
  short_description: "Merge or deploy completed work with reusable evidence"
  default_prompt: "Use $solo-ship to reuse valid evidence and fast-merge completed work by default; deploy only on explicit intent, using the narrowest affected runtime path."
