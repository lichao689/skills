interface:
  display_name: "Solo Ship"
  short_description: "Ship completed solo work through deployment and production verification"
  default_prompt: "Use $solo-ship to review, verify, publish, merge, deploy, validate, and safely clean up the completed branch."
