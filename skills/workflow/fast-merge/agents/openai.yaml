interface:
  display_name: "Fast Merge"
  short_description: "Local-first integration for completed personal work"
  default_prompt: "Use $fast-merge to integrate verified work into local main. Push, open a PR, or deploy on this machine only if I explicitly ask."
